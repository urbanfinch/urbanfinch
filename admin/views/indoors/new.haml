.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:indoors, :index)
      %li.active=link_to pat(:new), url(:indoors, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:indoor)
    .inner
      -form_for :indoor, url(:indoors, :create), :class => :form do |f|
        =partial "indoors/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
