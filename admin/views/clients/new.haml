.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:clients, :index)
      %li.active=link_to pat(:new), url(:clients, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:client)
    .inner
      -form_for :client, url(:clients, :create), :class => :form do |f|
        =partial "clients/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
